using UnityEngine;
using System.Collections;

public class ScenePlankton : MonoBehaviour 
{
	// Use this for initialization
	void Start () 
	{
		// currently this component is just used to tag a gameObject, so we don't have to rely on naming
	}
}
